<<<<<<< HEAD
name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 16
        uses: actions/setup-java@v2
        with:
          distribution: "adopt"
          java-version: 16
      - uses: actions/setup-node@v2
        with:
          node-version: "14"
          check-latest: true
      - name: Change wrapper permissions
        run: chmod +x ./gradlew
      - name: clean build
        run: ./gradlew clean build --stacktrace
=======
name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 16
        uses: actions/setup-java@v2
        with:
          distribution: "adopt"
          java-version: 16
      - name: Change wrapper permissions
        run: chmod +x ./gradlew
      - name: clean build
        run: ./gradlew clean build --stacktrace
>>>>>>> 1c5e19224430de27304d5bf3a88aaa2a2a6dbe91
