import { createStore } from "vuex";
import user from "./user";
import topic from "./topic";

export default createStore({
	state: {},

	mutations: {},

	actions: {},

	modules: {
		user,
		topic
	}
});
